#include <iostream>
using namespace std;

int main()
{
   int a, b, c;
   cin >> a >> b >> c;
   if ((a + b) % c == 0 ) 
   {
       if (c % b == 0) cout << ((a+b)/c) - (c/b);
       else if (c % b != 0) cout << ((a+b)/c) + (b*c);
   }
   else cout << (a - b + c);

    return 0;
}
